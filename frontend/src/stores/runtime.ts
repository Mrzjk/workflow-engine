import {defineStore} from 'pinia'; import {ref} from 'vue'; export const useRuntimeStore=defineStore('runtime',()=>{const runId=ref(''),events=ref<any[]>([]),nodeStatuses=ref<Record<string,string>>({}),nodeOutputs=ref<Record<string,unknown>>({}),llmStreaming=ref<Record<string,string>>({}); const connect=(id:string)=>{runId.value=id;const es=new EventSource(`/api/workflows/x/runs/${id}/stream`);es.onmessage=e=>{const x=JSON.parse(e.data);events.value.push(x);if(x.node_id)nodeStatuses.value[x.node_id]=x.event;if(x.event==='llm_token')llmStreaming.value[x.node_id]=(llmStreaming.value[x.node_id]||'')+x.data.content;if(x.event==='workflow_finished')es.close()}};return{runId,events,nodeStatuses,nodeOutputs,llmStreaming,connect}})
